---
description: Pi Agent Skills 开发完整指南。Pi 实现了 Agent Skills 标准，skills 是自包含的能力包，包含 SKILL.md 指令文件、辅助脚本和参考文档。用于创建、调试和发布 Pi skills。
globs: **/skills/**/SKILL.md, **/.pi/skills/**/*
---
# Pi Skills 开发指南

## Skills 是什么

Skills 是自包含的能力包，agent 按需加载。一个 skill 提供专用工作流、设置说明、辅助脚本和特定任务的参考文档。

Pi 实现了 [Agent Skills 标准](https://agentskills.io/specification)。

## 加载位置

Pi 从以下位置加载 skills：

- **全局：**
  - `~/.pi/agent/skills/`
  - `~/.agents/skills/`
- **项目：**
  - `.pi/skills/`
  - `.agents/skills/`（在 cwd 和祖先目录中，向上到 git 仓库根目录或文件系统根目录）
- **包：** `skills/` 目录或 `package.json` 中的 `pi.skills` 条目
- **设置：** `skills` 数组（文件或目录）
- **CLI：** `--skill <path>`（可重复，即使在 `--no-skills` 下也生效）

### 发现规则

- 在 `~/.pi/agent/skills/` 和 `.pi/skills/` 中，根目录下的 `.md` 文件被当作独立 skill
- 在所有 skill 位置中，包含 `SKILL.md` 的目录被递归发现
- 在 `~/.agents/skills/` 和 `.agents/skills/` 中，根目录的 `.md` 文件被忽略

使用 `--no-skills` 禁用发现（`--skill` 明确路径仍然加载）。

### 复用其他工具的技能

```json
// ~/.pi/agent/settings.json 或 .pi/settings.json
{
  "skills": [
    "~/.claude/skills",
    "~/.codex/skills"
  ]
}
```

## Skill 结构

```
my-skill/
├── SKILL.md              # 必需：frontmatter + 指令
├── scripts/              # 辅助脚本
│   └── process.sh
├── references/           # 按需加载的详细文档
│   └── api-reference.md
└── assets/
    └── template.json
```

## SKILL.md 格式

```markdown
---
name: my-skill
description: 这个 skill 做什么以及何时使用。要具体。
---

# My Skill

## Setup

首次使用前运行：
```bash
cd /path/to/skill && npm install
```

## Usage

```bash
./scripts/process.sh <input>
```
```

使用相对路径引用 skill 目录中的资源：

```markdown
详情请参见 [参考指南](references/REFERENCE.md)。
```

## Frontmatter 字段

| 字段 | 必需 | 说明 |
|------|------|------|
| `name` | 是 | 最大 64 字符。小写 a-z, 0-9, 连字符。Pi 不需要与父目录名一致 |
| `description` | 是 | 最大 1024 字符。做什么和何时使用 |
| `license` | 否 | 许可证名称或引用 |
| `compatibility` | 否 | 最大 500 字符。环境要求 |
| `metadata` | 否 | 任意键值映射 |
| `allowed-tools` | 否 | 空格分隔的预批准工具（实验性） |
| `disable-model-invocation` | 否 | `true` 时从系统提示隐藏，用户必须使用 `/skill:name` |

### Name 规则

- 1-64 字符
- 小写字母、数字、连字符
- 无首尾连字符
- 无连续连字符

✅ 正确：`pdf-processing`, `data-analysis`, `code-review`
❌ 错误：`PDF-Processing`, `-pdf`, `pdf--processing`

### Description 最佳实践

✅ 好的描述：
```yaml
description: Extracts text and tables from PDF files, fills PDF forms, and merges multiple PDFs. Use when working with PDF documents.
```

❌ 差的描述：
```yaml
description: Helps with PDFs.
```

## Skill 命令

Skills 注册为 `/skill:name` 命令：

```bash
/skill:brave-search           # 加载并执行 skill
/skill:pdf-tools extract      # 加载 skill 并传递参数
```

参数会追加到 skill 内容后，格式为 `User: <args>`。

在交互模式中用 `/settings` 或在 `settings.json` 中切换：

```json
{
  "enableSkillCommands": true
}
```

## 如何工作

1. 启动时，Pi 扫描 skill 位置并提取名称和描述
2. 系统提示包含可用 skills 的 XML 列表
3. 当任务匹配时，agent 使用 `read` 加载完整的 SKILL.md
4. Agent 遵循指令，使用相对路径引用脚本和资产

这是渐进式披露：只有描述始终在上下文中，完整指令按需加载。

## 验证

Pi 会根据 Agent Skills 标准验证 skills。大多数问题产生警告但仍会加载：

- 名称超过 64 字符或包含无效字符
- 名称以连字符开头/结尾或有连续连字符
- 描述超过 1024 字符

未知的 frontmatter 字段会被忽略。

**例外：** 缺少描述的 skill 不会被加载。

名称冲突（不同位置的同名 skill）会发出警告并保留找到的第一个。

## 示例

```
~/.pi/agent/skills/brave-search/
├── SKILL.md
├── search.js
└── content.js
```

**SKILL.md:**

```markdown
---
name: brave-search
description: Web search and content extraction via Brave Search API. Use for searching documentation, facts, or any web content.
---

# Brave Search

## Setup
```bash
cd /path/to/brave-search && npm install
```

## Search
```bash
./search.js "query"
./search.js "query" --content
```

## Extract Page Content
```bash
./content.js https://example.com
```
```

## Skill 仓库参考

- [Anthropic Skills](https://github.com/anthropics/skills) — 文档处理 (docx, pdf, pptx, xlsx)、Web 开发
- [Pi Skills](https://github.com/badlogic/pi-skills) — Web 搜索、浏览器自动化、Google API、转录
- [Agent Skills 规范](https://agentskills.io/specification)

## 创建 Skill 的步骤

1. 在 `~/.pi/agent/skills/` 或 `.pi/skills/` 下创建目录
2. 创建 `SKILL.md` 文件，包含 frontmatter（name, description）和指令
3. 添加辅助脚本到 `scripts/`、文档到 `references/`、资源到 `assets/`
4. 确保脚本有执行权限
5. 运行 `/reload` 或重启 Pi
6. 使用 `/skill:name` 或让 agent 自动检测加载

## 安全提示

> **安全：** Skills 可以指示模型执行任何操作，并可能包含模型调用的可执行代码。使用前请审查 skill 内容。