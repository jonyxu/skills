---
description: Pi Extensions 开发完整指南。Extensions 是 TypeScript 模块，可订阅生命周期事件、注册自定义工具、添加命令和自定义 UI。涵盖 API 参考、事件系统、工具注册、自定义 UI 和包分发。
globs: .pi/extensions/**/*, ~/.pi/agent/extensions/**/*
---
# Pi Extensions 开发指南

Extensions 是 TypeScript 模块，用于扩展 Pi 的行为。可以订阅生命周期事件、注册 LLM 可调用的自定义工具、添加命令等。

## 快速开始

创建 `~/.pi/agent/extensions/my-extension.ts`：

```typescript
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import { Type } from "typebox";

export default function (pi: ExtensionAPI) {
  // 响应事件
  pi.on("session_start", async (_event, ctx) => {
    ctx.ui.notify("Extension loaded!", "info");
  });

  // 拦截工具调用
  pi.on("tool_call", async (event, ctx) => {
    if (event.toolName === "bash" && event.input.command?.includes("rm -rf")) {
      const ok = await ctx.ui.confirm("Dangerous!", "Allow rm -rf?");
      if (!ok) return { block: true, reason: "Blocked by user" };
    }
  });

  // 注册自定义工具
  pi.registerTool({
    name: "greet",
    label: "Greet",
    description: "Greet someone by name",
    parameters: Type.Object({
      name: Type.String({ description: "Name to greet" }),
    }),
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      return {
        content: [{ type: "text", text: `Hello, ${params.name}!` }],
        details: {},
      };
    },
  });

  // 注册命令
  pi.registerCommand("hello", {
    description: "Say hello",
    handler: async (args, ctx) => {
      ctx.ui.notify(`Hello ${args || "world"}!`, "info");
    },
  });
}
```

测试：
```bash
pi -e ./my-extension.ts
```

## 扩展位置

| 位置 | 范围 |
|------|------|
| `~/.pi/agent/extensions/*.ts` | 全局 |
| `~/.pi/agent/extensions/*/index.ts` | 全局（子目录） |
| `.pi/extensions/*.ts` | 项目 |
| `.pi/extensions/*/index.ts` | 项目（子目录） |

更多路径通过 `settings.json`：

```json
{
  "extensions": [
    "/path/to/local/extension.ts",
    "/path/to/local/extension/dir"
  ]
}
```

## 可用导入

| 包 | 用途 |
|----|------|
| `@earendil-works/pi-coding-agent` | 类型 (`ExtensionAPI`, `ExtensionContext`, 事件) |
| `typebox` | 工具参数模式定义 |
| `@earendil-works/pi-ai` | AI 工具 (`StringEnum`) |
| `@earendil-works/pi-tui` | TUI 组件 |
| `node:fs`, `node:path` | Node 内置 |

npm 依赖也有效。在扩展旁边（或父目录）添加 `package.json`，运行 `npm install`。

## 扩展样式

### 单文件
```
~/.pi/agent/extensions/my-extension.ts
```

### 目录 + index.ts
```
~/.pi/agent/extensions/my-extension/
├── index.ts
├── tools.ts
└── utils.ts
```

### 带依赖的包
```
~/.pi/agent/extensions/my-extension/
├── package.json
├── package-lock.json
├── node_modules/
└── src/
    └── index.ts
```

`package.json`:
```json
{
  "name": "my-extension",
  "dependencies": {
    "zod": "^3.0.0",
    "chalk": "^5.0.0"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  }
}
```

## 事件系统

### 生命周期

```
pi starts
  │
  ├─► session_start { reason: "startup" }
  └─► resources_discover { reason: "startup" }
      │
      ▼
user sends prompt ─────────────────────────┐
  │                                        │
  ├─► (extension commands checked first)   │
  ├─► input (intercept/transform)          │
  ├─► before_agent_start (inject msg)      │
  ├─► agent_start                          │
  │                                        │
  │   turn (repeats while LLM calls tools) │
  │   ├─► turn_start                       │
  │   ├─► context (modify messages)        │
  │   ├─► before_provider_request          │
  │   ├─► after_provider_response          │
  │   │                                    │
  │   │   LLM responds, may call tools:    │
  │   │     ├─► tool_execution_start       │
  │   │     ├─► tool_call (can block)      │
  │   │     ├─► tool_result (modify)       │
  │   │     └─► tool_execution_end         │
  │   │                                    │
  │   └─► turn_end                         │
  │                                        │
  └─► agent_end                            │
                                           │
user sends another prompt ◄────────────────┘
```

### 关键事件

#### session_start
```typescript
pi.on("session_start", async (event, ctx) => {
  // event.reason: "startup" | "reload" | "new" | "resume" | "fork"
  // event.previousSessionFile: 存在时表示来源
});
```

#### resources_discover
```typescript
pi.on("resources_discover", async (event, _ctx) => {
  // event.cwd, event.reason: "startup" | "reload"
  return {
    skillPaths: ["/path/to/skills"],
    promptPaths: ["/path/to/prompts"],
    themePaths: ["/path/to/themes"],
  };
});
```

#### before_agent_start
```typescript
pi.on("before_agent_start", async (event, ctx) => {
  // event.prompt - 用户提示
  // event.systemPrompt - 当前连锁系统提示
  // event.systemPromptOptions - 系统提示构建选项
  return {
    message: {
      customType: "my-ext",
      content: "Additional context for LLM",
      display: true,
    },
    systemPrompt: event.systemPrompt + "\n\nExtra instructions...",
  };
});
```

#### tool_call (拦截/阻止)
```typescript
pi.on("tool_call", async (event, ctx) => {
  // event.toolName, event.input
  return { block: true, reason: "Blocked by extension" };
  // 或返回 undefined 允许继续
});
```

#### context (修改消息)
```typescript
pi.on("context", async (event, ctx) => {
  // event.messages - 深拷贝，安全修改
  const filtered = event.messages.filter(m => !shouldPrune(m));
  return { messages: filtered };
});
```

#### session_before_compact / session_compact
```typescript
pi.on("session_before_compact", async (event, ctx) => {
  return { cancel: true };
  // 或自定义摘要
  return { compaction: { summary: "...", firstKeptEntryId, tokensBefore } };
});
```

## ExtensionAPI 方法

### 事件处理
- `pi.on(event, handler)` — 订阅事件
- `pi.events` — EventEmitter 实例（高级用法）

### 工具注册
```typescript
pi.registerTool({
  name: "my-tool",
  label: "My Tool",
  description: "Description for LLM",
  parameters: Type.Object({
    input: Type.String({ description: "Input text" }),
  }),
  async execute(toolCallId, params, signal, onUpdate, ctx) {
    // onUpdate({ type: "text", text: "progress..." }) — 流式更新
    return {
      content: [{ type: "text", text: `Result: ${params.input}` }],
      details: {},
    };
  },
});
```

### 消息发送
```typescript
pi.sendMessage(message, options?);
pi.sendUserMessage(content, options?);
pi.appendEntry(customType, data?);  // 持久化自定义数据
```

### 会话管理
```typescript
pi.setSessionName(name);
pi.getSessionName();
pi.setLabel(entryId, label);
```

### 命令注册
```typescript
pi.registerCommand("name", {
  description: "Description",
  handler: async (args, ctx) => {
    // ctx 是 ExtensionCommandContext
    // ctx.ui 用于用户交互
    // ctx.newSession(), ctx.fork(), ctx.switchSession()
    // ctx.reload()
  },
});
```

### UI 交互
```typescript
// 在 ctx.ui 上可用的方法
ctx.ui.notify("message", "info");       // 通知
ctx.ui.confirm("Title", "Question?");   // 确认对话框
ctx.ui.select("Title", "Prompt", [      // 选择列表
  { label: "Option A", value: "a" },
  { label: "Option B", value: "b" },
]);
ctx.ui.input("Title", "Prompt", "default");
ctx.ui.setStatus("my-ext", "Processing...");  // footer 状态
ctx.ui.setWidget("my-ext", ["Line 1", "Line 2"]);  // 编辑器上方 widget
```

### 快捷键和标志
```typescript
pi.registerShortcut("ctrl+x", {
  description: "Shortcut description",
  handler: async (ctx) => { ... },
});

pi.registerFlag("my-flag", {
  description: "Flag description",
  type: "string",
  handler: async (value, ctx) => { ... },
});
```

### 模型和工具控制
```typescript
pi.getActiveTools() / pi.getAllTools() / pi.setActiveTools(names);
pi.setModel(model);
pi.getThinkingLevel() / pi.setThinkingLevel(level);
```

### Provider 注册
```typescript
pi.registerProvider("custom-provider", {
  baseUrl: "http://localhost:1234/v1",
  apiKey: "$API_KEY",
  api: "openai-completions",  // 或其他 API 格式
  models: [{
    id: "model-id",
    name: "Model Name",
    reasoning: false,
    input: ["text"],
    cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
    contextWindow: 128000,
    maxTokens: 4096,
  }],
});

pi.unregisterProvider(name);
```

### 异步工厂函数
```typescript
export default async function (pi: ExtensionAPI) {
  const response = await fetch("http://localhost:1234/v1/models");
  const payload = await response.json();
  pi.registerProvider("local-openai", {
    baseUrl: "http://localhost:1234/v1",
    apiKey: "$LOCAL_OPENAI_API_KEY",
    api: "openai-completions",
    models: payload.data.map(model => ({
      id: model.id,
      name: model.name ?? model.id,
      reasoning: false,
      input: ["text"],
      cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
      contextWindow: model.context_window ?? 128000,
      maxTokens: model.max_tokens ?? 4096,
    })),
  });
}
```

## ExtensionCommandContext 方法

```typescript
ctx.getSystemPrompt();              // 获取当前系统提示
ctx.getSystemPromptOptions();       // 获取结构化系统提示选项
ctx.waitForIdle();                  // 等待空闲
ctx.newSession(options?);           // 创建新会话
ctx.fork(entryId, options?);        // 从指定条目分叉
ctx.navigateTree(targetId, options?);// 导航到树节点
ctx.switchSession(sessionPath, opts?);// 切换会话
ctx.reload();                       // 重载
```

## 自定义渲染

```typescript
pi.registerMessageRenderer("customType", (message, ctx) => {
  return content;  // 返回 Ink 元素
});
```

## 发布为 Pi 包

```json
{
  "name": "my-extension-pkg",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./extensions"]
  }
}
```

安装：
```bash
pi install npm:@scope/my-extension-pkg
pi install git:github.com/user/repo
```

## 示例用例

- 权限门（确认 `rm -rf`、`sudo` 等）
- Git 检查点（每回合 stash，分支切换时恢复）
- 路径保护（阻止写入 `.env`、`node_modules/`）
- 自定义压缩（用自己的方式总结对话）
- 对话摘要
- 交互式工具（问题、向导、自定义对话框）
- 有状态工具（待办列表、连接池）
- 外部集成（文件监视器、webhook、CI 触发器）
- 等待时的游戏

详细示例参见：https://github.com/earendil-works/pi/tree/main/packages/coding-agent/examples/extensions

## 安全提示

> **安全：** 扩展以您的完整系统权限运行，可执行任意代码。只安装来自可信源的扩展。
> 自动发现位置中的扩展可以通过 `/reload` 热重载。使用 `pi -e ./path.ts` 仅用于快速测试。